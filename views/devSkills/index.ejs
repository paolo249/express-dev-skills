<%- include('../partials/header') %>

    <h1>Paolo's Skill Set</h1>
    <ul>
      <% devSkills.forEach(function(t) { %>
        <li>
            <a href="/devSkills/<%= t.id %>">
          <%= t.devSkill %>
            - 
          <%= t.done ? 'done' : 'not done' %>
            </a>
        </li>
      <% }); %>
    </ul>
 

<%- include('../partials/footer') %>

